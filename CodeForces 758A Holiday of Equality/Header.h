//Header.h: Header file includes the prototypes of the custom functions used in the Header.c 

void readArray(int* pointer, int arraySize);
int maxArrayElement(int array[], int arraySize);
int totalBurles(int array[], int arraySize, int maxElement);
