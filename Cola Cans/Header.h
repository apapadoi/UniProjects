//Header.h: Header file includes the prototypes of the custom functions used in the Header.c 

void sort(int n, int* ptr);
char checkSum(int* pointerA, int* pointerB, int tableSize);
void readArray(int* pointer, int tableSize);
void swap(int* a, int* b);
int arraySum(int* pointer, int tableSize);
char checkArraysValues(int* pointerA, int* pointerB, int tableSize);
